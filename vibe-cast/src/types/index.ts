export interface WeatherData {
    temperature: number;
    condition: string;
    city: string;
}

export interface MusicRecommendation {
    songTitle: string;
    artist: string;
    mood: string;
}

export interface UserInput {
    city: string;
}